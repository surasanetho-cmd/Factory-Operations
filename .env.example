# Factory Operations — copy to .env.local (do not commit)

# Supabase project: Factory-Operations (ap-south-1)
# Dashboard → Project Settings → API
NEXT_PUBLIC_SUPABASE_URL=https://ilkzavjrjwjebcyitgaj.supabase.co

# Project Settings → API → anon / publishable key
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Project Settings → API → service_role (server only — never NEXT_PUBLIC_)
SUPABASE_SERVICE_ROLE_KEY=
